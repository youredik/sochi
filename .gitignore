# Dependencies
node_modules/
.pnpm-store/

# Environment
.env
.env.local
.env.*.local

# Build output
dist/
build/
.output/
# PWA dev artifacts (vite-plugin-pwa generates SW + workbox в dev mode под dev-dist/).
dev-dist/

# IDE / editors
.vscode/
.idea/
.DS_Store
.claude/

# Tests
coverage/
.stryker-tmp/
reports/
playwright-report/
playwright/.cache/
test-results/
tests/.auth/
.vitest-attachments/

# Docker data volumes (for reference — real ones are in Docker-managed volumes)
ydb_data/
minio_data/

# Terraform (when we get there)
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
planfile

# Build artifacts (videos, generated assets, reports — regenerable, not tracked)
.artifacts/
tmp/

# Lighthouse CI local cache — runs collected by `pnpm lhci:collect` (M9.widget.7 / A5.1).
# Reports rotate per-run + uploaded ad-hoc; not source-of-truth state.
.lighthouseci/

# Misc
*.log
.DS_Store
