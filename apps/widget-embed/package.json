{
	"name": "@horeca/widget-embed",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build",
		"build:size": "gzip-size dist/embed.js --raw",
		"build:check": "vite build && node scripts/check-size.mjs",
		"test": "vitest run"
	},
	"dependencies": {
		"lit": "3.3.2"
	},
	"devDependencies": {
		"gzip-size-cli": "^5.1.0",
		"rollup-plugin-visualizer": "^7.0.1",
		"terser": "^5.46.2",
		"vite": "^8.0.10",
		"vitest": "^4.1.5"
	}
}
