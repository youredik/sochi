{
	"name": "@horeca/widget-embed",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "rm -rf dist && vite build && EMBED_ENTRY=flow vite build",
		"build:check": "pnpm build && node scripts/check-size.mjs",
		"test": "vitest run",
		"test:browser": "vitest run --config vitest.browser.config.ts"
	},
	"dependencies": {
		"@horeca/shared": "workspace:*",
		"@lit-labs/ssr-client": "^1.1.8",
		"lit": "3.3.3"
	},
	"devDependencies": {
		"@vitest/browser-playwright": "^4.1.7",
		"rollup-plugin-visualizer": "^7.0.1",
		"terser": "^5.47.1",
		"vite": "^8.0.12",
		"vitest": "^4.1.7",
		"vitest-browser-lit": "^1.0.1"
	}
}
