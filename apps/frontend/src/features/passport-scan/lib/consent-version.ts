/**
 * Current 152-ФЗ consent text version. Bumped when legal text changes.
 * Used to detect стorter consents (старая version → re-prompt).
 */
export const CONSENT_152FZ_VERSION = '2026-04-28' as const
