# Backend Bun test config (Phase 16 — 2026-05-12).
# Replaces apps/backend/vitest.config.ts setupFiles entry.
#
# Preload entries — SEPARATE entries, no await chains (canon
# `feedback_bun_preload_race_2026_05_13.md`). Bun loads them sequentially
# в order; sharing modules between entries безопасно.
[test]
preload = [
  "./src/tests/env-defaults.ts",
  "./src/tests/preload-driver-mock.ts",
]
